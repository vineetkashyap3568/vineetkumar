flask
selenium
webdriver_manager
# Install Chromium and necessary dependencies for headless mode
RUN apt-get update && apt-get install -y \
    chromium \
    chromium-driver \
    libappindicator3-1 \
    fonts-liberation \
    libasound2 \
    libatk-bridge2.0-0 \
    libatk1.0-0 \
    libcups2 \
    libdbus-1-3 \
    libgdk-pixbuf2.0-0 \
    libnspr4 \
    libnss3 \
    libx11-xcb1 \
    libxcomposite1 \
    libxrandr2 \
    xdg-utils \
    && apt-get clean

